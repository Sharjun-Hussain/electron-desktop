export const metadata = {
  title: "Customer Purchase History | Inzeedo POS",
  description: "Developed By : Inzeedo (PVT) Ltd.",
};

export default function Layout({ children }) {
  return children;
}
