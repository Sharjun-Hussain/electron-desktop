export const metadata = {
  title: "Purchase History Report | Inzeedo POS",
  description: "Developed By : Inzeedo (PVT) Ltd.",
};

export default function Layout({ children }) {
  return children;
}
