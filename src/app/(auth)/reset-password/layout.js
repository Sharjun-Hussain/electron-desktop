export const metadata = {
  title: "Reset Password | Inzeedo POS",
  description: "Create a new secure password for your Inzeedo POS account. Secure your industrial terminal access today.",
};

export default function ResetPasswordLayout({ children }) {
  return children;
}
